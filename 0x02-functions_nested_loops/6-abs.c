#include "holberton.h"
/**
 * _abs - absolute value of the code.
 * @r: integer as an Absolute value
 * Return: Always 0.
 */

int _abs(int r)
{
if (r < 0)
{
r = r * -1;
return (r);
}
else
{
return (r);
}
}
